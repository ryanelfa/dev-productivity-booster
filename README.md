# dev-productivity-booster
Small proof-of-concept showing how AI (GitHub copilot) and automation (GitHub Actions) can enhance developer productivity.

## 📁 Data
The dataset lives in `data/productivity.csv` with the following schema:

- `developer` (str)
- `commits` (int)
- `tests_passed` (int)
- `bugs_fixed` (int)
- `lines_added` (int)

Values are synthetic, for demonstration purposes only.